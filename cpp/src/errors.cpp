#include <retro_metadata/errors.hpp>

// All error implementations are inline in the header file.
// This file exists to ensure the errors module is included in the build.

namespace retro_metadata {

// Force instantiation of error types to ensure they're available
// when linking the library.

}  // namespace retro_metadata
